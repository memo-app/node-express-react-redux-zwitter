{
    "port": 5001,
    "databaseUrl": "mongodb://localhost/memoapp",
    "saltRounds": 10,
    "authentication": {
        "token": {
            "secret": "super-secret-key",
            "issuer": "MemoApp",
            "audience": "MemoApp"
        }
    }
}